extends layout

block content
  h1= title
  p Welcome to #[em LocalLibrary], a very basic Express website, using MongoDB as the database (hosted on Atlas) and Mongoose as the ODM.

  h1 Dynamic content

  p The library has the following record counts:

  ul
    li #[strong Books:] !{data[0]}
    li #[strong Copies:] !{data[3]}
    li #[strong Copies available:] !{data[4]}
    li #[strong Authors:] !{data[1]}
    li #[strong Genres:] !{data[2]}